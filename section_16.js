export function dynamicOperations(){
  const getProperty = (obj, key) => {

  }

  const setProperty = (obj, key, value) => {

  }

  const hasProperty = (obj, key) => {

  }

  const deleteProperty = (obj, key) => {

  }

  const listKeys = (obj) => {
      
  }
}